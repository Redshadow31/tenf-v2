"use client";

import MembersHubView from "@/components/admin/members-hub/MembersHubView";

/**
 * Hub `/admin/membres` — pilotage communauté (cockpit zinc/violet).
 */
export default function MembersHubPage() {
  return <MembersHubView />;
}
